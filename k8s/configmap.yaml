apiVersion: v1
kind: ConfigMap
metadata:
  name: release-guardian-config
data:
  APP_NAME: release-guardian
  NODE_ENV: production
  PORT: "3000"
  RISK_THRESHOLD: medium
  DB_PATH: /app/data/runtime.db
